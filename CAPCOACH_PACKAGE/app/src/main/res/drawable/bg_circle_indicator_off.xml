<?xml version="1.0" encoding="utf-8"?>
<!-- Whitish circle for the unselected radio indicator (clickable affordance). -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="@color/surface_container_highest" />
    <stroke
        android:width="@dimen/stroke_thin"
        android:color="@color/outline_dark" />
</shape>
